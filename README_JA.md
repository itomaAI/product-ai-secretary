
# MetaOS v2.2 (System 0)

**ブラウザ上で完結して動作する、インテリジェントなAI秘書システム**

Powered by [MetaForge](https://github.com/itomaAI/product-auto-app-builder) (The AI App Builder).

---

## 🚀 概要

MetaOSは単なるダッシュボードではありません。AIによって管理される「生きているシステム」です。
従来のアプリのように手動でデータを入力するだけでなく、AI秘書に「これをやっておいて」と伝えるだけで、AIが裏側でシステムファイルを更新し、タスクやスケジュールを管理します。

### キーコンセプト
- **Host-Driven Intelligence（ホスト主導の知能）**: 「脳（MetaForge AI）」はホスト環境に存在し、「体（MetaOS Dashboard）」を操作します。
- **Single-Tenant Architecture**: 使い捨てのプロジェクトではなく、長く付き合うパートナーとして設計されています。「タイムマシン機能」により、いつでも過去の状態に戻せます。
- **Local Execution**: ブラウザ技術（IndexedDB, VFS）のみを使用し、100%クライアントサイドで動作します。バックエンドサーバーは不要です。

---

## ✨ 機能

### 1. インテリジェントなタスク管理
- **チャット操作**: AIに「明日10時に佐藤さんとの会議を追加して」「レポート作成のタスクを完了にして」と話しかけるだけで操作できます。
- **ダッシュボード**: タスクの確認、チェックボックスによる完了、クイック追加が可能なクリーンなUI。
- **分散データ保存**: タスクデータは月ごとのJSONファイル（`data/tasks/YYYY-MM.json`）に分割保存され、長期間の使用にも耐えられます。

### 2. アプリ & ツール
- **Calendar**: イベントデータベースと連動した月表示カレンダー。
- **Notes**: 自動保存機能付きのMarkdownノートアプリ。
- **Monaco Editor**: ホスト側にはVS Code互換の高性能エディタを搭載。システムファイルを直接編集できます。

### 3. タイムマシン (バックアップ & 復旧)
- **自動バックアップ**: 30分ごとにシステムの状態を自動保存します。
- **スマートな整理（Pruning）**: 古いバックアップは自動的に整理されます（24時間以降は1日1件、2週間で削除）。
- **インスタント復元**: いつでもOS全体を過去の特定の時点の状態に巻き戻せます。

### 4. 自由な拡張と自己構築
- **あなた自身が開発者**: ダッシュボードやアプリの実体は、VFS上の単なるHTML/JSファイルです。Monaco Editorを使っていつでも改造できます。
- **AIによるコーディング**: AIに機能追加を依頼できます。
    - 「右上に時計ウィジェットを追加して」
    - 「家計簿をつけるための新しい画面を作って」
    - AIがコードを書き換え、MetaOSは即座に進化します。

---

## 🛠️ 使い方

### 最初の一歩
1.  **Factory Reset**: 初回起動時や画面表示がおかしい場合は、サイドバーの「赤いリセットアイコン」をクリックしてファイルシステムを初期化してください。
2.  **Explore**: サイドバーからアプリ（Dashboard, Calendar, Notes）にアクセスできます。

### AIとの対話
右側のチャットパネルで自然に話しかけてください。
- 「牛乳を買うタスクを追加して」
- 「今月の予定を教えて」
- 「プロジェクト計画についてのノートを作成して」

### 手動操作
- **ファイル編集**: ファイルエクスプローラーから `data/` 内のファイルを開き、JSONを直接編集できます。変更は即座にダッシュボードに反映されます。
- **バックアップ**: 大きな変更を加える前には、「保存アイコン」をクリックして手動スナップショットを作成することをお勧めします。

---

## 🏗️ 技術アーキテクチャ

### スタック
- **Engine**: MetaForge v2.2 (Gemini-powered LLM loop).
- **Storage**: IndexedDBを利用した仮想ファイルシステム (VFS)。
- **View**: サンドボックス化された `iframe` 内で動作する HTML/JS アプリケーション。

### ブリッジシステム
ホスト（MetaForge）とゲスト（MetaOS）は `MetaOS Bridge` を通じて通信します：
1.  **Host -> Guest**: AIやエディタがデータを変更すると `file_changed` イベントを送信。ゲスト側は「ホットアップデート（リロードなしの再描画）」を行います。
2.  **Guest -> Host**: ダッシュボードでの操作内容は `save_file` リクエストとしてホストに送信され、保存されます。

---

*This project demonstrates the potential of "Self-Modifying AI Applications".*
